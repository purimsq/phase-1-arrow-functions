// Function expression
const divide = function(a, b) {
    return a / b;
  };

  // Arrow function
  const square = (num) => {
    return num * num;
  };

  // Arrow function
  const add = (a, b) => {
    return a + b;
  };

  // Export the functions
  module.exports = { divide, square, add };